import { Typography } from "@mui/material";

export const FavoritesScreen = () => {
    return (
        <Typography variant="h1" color="initial">
            Favoritados
        </Typography>
    );
}